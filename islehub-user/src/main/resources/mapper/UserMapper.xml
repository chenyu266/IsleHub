<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.islehub.user.mapper.UserMapper">

    <select id="pageUsers" resultType="com.islehub.user.entity.User">
        SELECT id, username, email, phone, avatar, last_login_time, last_login_ip, role, status, created_at, updated_at FROM user
        WHERE delete_flag = 0
        <if test="keyword != null and keyword != ''">
            AND (username LIKE CONCAT('%', #{keyword}, '%')
                 OR phone LIKE CONCAT('%', #{keyword}, '%'))
        </if>
        ORDER BY created_at DESC
    </select>

</mapper>
